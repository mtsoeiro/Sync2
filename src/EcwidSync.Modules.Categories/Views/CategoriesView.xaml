<UserControl x:Class="EcwidSync.Modules.Categories.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:EcwidSync.Modules.Categories.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CategoriesViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!-- toolbar opcional -->
        
        <TreeView ItemsSource="{Binding Roots}" x:Name="CatTree"
                  AllowDrop="True" ScrollViewer.CanContentScroll="True"
                  Grid.Row="0">
            <TreeView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </TreeView.ItemsPanel>

            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="HorizontalContentAlignment"
                            Value="{Binding ElementName=CatTree, Path=HorizontalContentAlignment}"/>
                    <Setter Property="VerticalContentAlignment"
                            Value="{Binding ElementName=CatTree, Path=VerticalContentAlignment}"/>
                    <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
                    <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.Resources>
                <Style x:Key="NumericText" TargetType="TextBlock">
                    <Setter Property="FontFamily" Value="Segoe UI Variable, Bahnschrift"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Typography.NumeralAlignment" Value="Tabular"/>
                    <Setter Property="Typography.NumeralStyle" Value="Lining"/>
                </Style>

                <HierarchicalDataTemplate DataType="{x:Type vm:CategoryTreeNode}"
                                          ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" ToolTipService.ShowDuration="8000">
                        <TextBlock Text="[" Opacity="0.6" Style="{StaticResource NumericText}" />
                        <TextBlock Text="{Binding ProductCount}" Opacity="0.85" Style="{StaticResource NumericText}" />
                        <TextBlock Text="] " Opacity="0.6" Style="{StaticResource NumericText}" />
                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Enabled}" Value="False">
                                            <Setter Property="Opacity" Value="0.6"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ToolTipService.ToolTip>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Id, StringFormat=ID: {0}}"/>
                                <TextBlock Text="{Binding Updated, StringFormat=Atualizado: {0:yyyy-MM-dd HH:mm}}"/>
                            </StackPanel>
                        </ToolTipService.ToolTip>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Carregar TUDO" Command="{Binding LoadAllCommand}" />
            <Button Content="Expandir tudo"  Command="{Binding ExpandAllCommand}" />
            <Button Content="Colapsar tudo"  Command="{Binding CollapseAllCommand}" />
            <TextBlock Margin="16,0,0,0" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="Pronto"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Text" Value="A carregar"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

    </Grid>
</UserControl>
