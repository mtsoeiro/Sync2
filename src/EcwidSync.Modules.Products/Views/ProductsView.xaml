<UserControl x:Class="EcwidSync.Modules.Products.Views.ProductsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:conv="clr-namespace:EcwidSync.Modules.Products.Converters">
    <UserControl.Resources>
        <conv:BoolToVisConverter x:Key="BoolToVis"/>
        <Style x:Key="Pill" TargetType="Border">
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="#22FFFFFF"/>
            <Setter Property="BorderBrush" Value="#55FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="6" >
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition MaxHeight="300" MinHeight="250"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal"  Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="5"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="5"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <ui:Button Grid.Column="1"  Command="{Binding LoadAllCommand}" Height="70" Width="100" ToolTip="Atualizar do Ecwid" Appearance="Primary"  >
                    <StackPanel VerticalAlignment="Center" MinWidth="40" HorizontalAlignment="Center">
                        <ui:SymbolIcon ui:Symbol="DatabaseLightning20" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                        <ui:TextBlock Text="Atualizar" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0" Grid.Row="1" Margin="0,0,0,0"/>
                    </StackPanel>
                </ui:Button>

                <ui:TextBox Grid.Column="3"  Width="280" Height="40" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                <ui:InfoBar Grid.Column="3" IsOpen="{Binding IsLoading}" Message="A carregar…" Severity="Success"   ></ui:InfoBar>


                <!--
                <Button Content="Limpar" Command="{Binding ClearSearchCommand}" />
                <TextBlock Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Pronto"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Text" Value="A carregar…"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>-->
            </Grid>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" Grid.Column="0" ItemsSource="{Binding ProductsView}" SelectedItem="{Binding Selected, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" EnableRowVirtualization="True"
                  EnableColumnVirtualization="True" SelectionMode="Single" Margin="0,6,8,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="*"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Preço" Binding="{Binding Price}" Width="*" />
                <DataGridTextColumn Header="Qtd" Binding="{Binding Quantity}" Width="*"/>
                <DataGridCheckBoxColumn Header="Ativo" Binding="{Binding Enabled}" Width="*"/>
                <DataGridTextColumn Header="Atualizado" Binding="{Binding Updated, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Detalhes -->
        <Border Grid.Row="2" Grid.Column="1" Padding="8" CornerRadius="12" BorderBrush="White" BorderThickness="1">
            <!-- Painel de detalhes (ajuste Grid.Column/Row conforme a sua layout) -->
            <ui:Card Grid.Column="1" Margin="8" Padding="12">
                <Grid DataContext="{Binding SelectedDetails}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Imagem -->
                    <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"
                    Width="160" Height="160" CornerRadius="8" Background="#1AFFFFFF">
                        <Image Stretch="Uniform" Source="{Binding ImageUrl}"/>
                    </Border>

                    <!-- Título + pills -->
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,8">
                        <TextBlock Text="{Binding Name}" FontSize="16"
                           FontWeight="SemiBold" TextWrapping="Wrap"/>

                        <WrapPanel Margin="0,6,0,0">
                            <Border Style="{StaticResource Pill}">
                                <TextBlock Text="{Binding Sku}"/>
                            </Border>
                            <Border Style="{StaticResource Pill}">
                                <TextBlock Text="{Binding Price, StringFormat={}{0:C}}"/>
                            </Border>
                            <Border Style="{StaticResource Pill}">
                                <TextBlock Text="{Binding Enabled, StringFormat=Ativo: {0}}"/>
                            </Border>
                            <Border Style="{StaticResource Pill}">
                                <TextBlock Text="{Binding InStock, StringFormat=Stock: {0}}"/>
                            </Border>
                        </WrapPanel>

                        <!-- Link para a loja (WPF) -->
                        <TextBlock Margin="0,6,0,0">
                    <Hyperlink NavigateUri="{Binding Url}" RequestNavigate="Hyperlink_RequestNavigate">
                        Abrir na loja
                    </Hyperlink>
                        </TextBlock>
                    </StackPanel>

                    <!-- Categorias -->
                    <Expander Grid.Row="1" Grid.Column="1" Header="Categorias" IsExpanded="True">
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=., Mode=OneWay, StringFormat=• {0}}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>

                    <!-- Atributos -->
                    <Expander Grid.Row="2" Grid.ColumnSpan="2" Header="Atributos" Margin="0,8,0,0">
                        <ItemsControl ItemsSource="{Binding Attributes}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <TextBlock Text=": " />
                                        <TextBlock Text="{Binding Value}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>

                    <!-- Opções -->
                    <Expander Grid.Row="3" Grid.ColumnSpan="2" Header="Opções" Margin="0,8,0,0">
                        <ItemsControl ItemsSource="{Binding Options}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,6">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                        <ItemsControl ItemsSource="{Binding Choices}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock>
                                                <Run Text="• "/><Run Text="{Binding}"/>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>

                    <!-- Descrição (texto limpo do HTML) -->
                    <Expander Grid.Row="2" Grid.Column="1" Header="Descrição" Margin="8,0,0,0" IsExpanded="True">
                        <ScrollViewer MaxHeight="220">
                            <TextBlock Text="{Binding DescriptionText}" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Expander>

                    <!-- Overlay de loading -->
                    <Grid Grid.RowSpan="4" Grid.ColumnSpan="2" Background="#66000000"
                  Visibility="{Binding DataContext.DetailsLoading,
                                       RelativeSource={RelativeSource AncestorType=ui:Card},
                                       Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="A carregar detalhes…" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </ui:Card>
        </Border>
    </Grid>
</UserControl>
